@model Order

<center>
    <h1>Thank you for placing the order</h1>
    @if (Model != null)
    {
        <a href="@Url.Action("DownloadPdf", new { orderId = Model.OrderId })" class="btn btn-success mb-3">Download Orders Pdf</a>
    }
    else
    {
        <p>Order not found.</p>
    }
</center>
